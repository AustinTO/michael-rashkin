# Repo skills for Codex (.agents/skills)

These skills follow the OpenAI/Codex “Agent Skills” format: each skill is a folder containing `SKILL.md` with YAML frontmatter (`name`, `description`) plus optional `scripts/`, `references/`, and `assets/`.

Codex scans `.agents/skills` folders from your current working directory up to the repo root.
