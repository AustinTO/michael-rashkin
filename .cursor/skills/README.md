# Project skills for Cursor (.cursor/skills)

Cursor discovers project skills in `.cursor/skills/<skill-name>/SKILL.md`.

These skills mirror the Codex skills under `.agents/skills` so you can use them in Cursor Agent chat via `/skill-name`.
